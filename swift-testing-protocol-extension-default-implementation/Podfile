source 'https://github.com/CocoaPods/Specs.git'

target 'ProtocolDefaultImplementation' do
    use_frameworks!

    target 'ProtocolDefaultImplementationTests' do
        inherit! :search_paths

        pod 'Quick'
        pod 'Nimble'
    end
end
